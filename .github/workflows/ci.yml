name: CI

on:
  pull_request:
  push:
    branches-ignore: [main]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'
      - name: Build data files and downloads
        run: npm run build
      - name: Static verification (data, sources, schema, demo acceptance + snapshot, secrets scan)
        run: npm run verify:static
      - name: Generated files are up to date (data/ and downloads/)
        run: |
          git diff --exit-code -- data/ downloads/ || {
            echo "::error::data/ 或 downloads/ 與 docs/、skills/、examples/ 不一致，請執行 npm run build 後提交"
            exit 1
          }
      - name: Python skills compile and dry-run (no API keys)
        run: |
          python3 -m py_compile skills/*/api-workflow.py skills/claude/preemptive-exposure-analysis/scripts/validate_inputs.py
          for p in chatgpt grok glm deepseek; do python3 skills/$p/api-workflow.py --dry-run >/dev/null; done
          python3 skills/claude/preemptive-exposure-analysis/scripts/validate_inputs.py examples/synthetic-org

  browser:
    runs-on: ubuntu-latest
    needs: verify
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
      - name: Install Playwright (from package-lock.json) and Chromium
        run: |
          npm ci
          npx playwright install --with-deps chromium
      - name: Build data files and downloads
        run: npm run build
      - name: Browser verification (navigation, search, filters, copy, download, case interaction, mobile, a11y)
        run: npm run verify
      - name: Upload verification report and screenshots
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: verify-report-and-screenshots
          path: |
            verify-report.md
            verify-screenshots/
          retention-days: 14
